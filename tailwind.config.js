module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Ensure all paths are correctly included
  theme: {
    extend: {
      colors: {
        // Custom colors if needed
        primary: '#1a202c',
        secondary: '#2d3748',
      },
    },
  },
  plugins: [],
}
